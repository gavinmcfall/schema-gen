{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://k8s-schemas.io/servicebinding.io/v1beta1/clusterworkloadresourcemapping.json",
  "title": "ClusterWorkloadResourceMapping",
  "description": "ClusterWorkloadResourceMapping is the Schema for the clusterworkloadresourcemappings API",
  "type": "object",
  "x-kubernetes-schema-metadata": {
    "sourceName": "service-binding",
    "sourceVersion": "v1.0.0",
    "extractedAt": "2025-12-14T02:28:51.048242+00:00",
    "generator": "k8s-schemas.io"
  },
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": "string"
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": "string"
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "description": "ClusterWorkloadResourceMappingSpec defines the desired state of ClusterWorkloadResourceMapping",
      "properties": {
        "versions": {
          "description": "Versions is the collection of versions for a given resource, with mappings.",
          "items": {
            "description": "ClusterWorkloadResourceMappingTemplate defines the mapping for a specific version of an workload resource to a\nlogical PodTemplateSpec-like structure.",
            "properties": {
              "annotations": {
                "description": "Annotations is a Restricted JSONPath that references the annotations map within the workload resource. These\nannotations must end up in the resulting Pod, and are generally not the workload resource's annotations.\nDefaults to `.spec.template.metadata.annotations`.",
                "type": "string"
              },
              "containers": {
                "description": "Containers is the collection of mappings to container-like fragments of the workload resource. Defaults to\nmappings appropriate for a PodSpecable resource.",
                "items": {
                  "description": "ClusterWorkloadResourceMappingContainer defines the mapping for a specific fragment of an workload resource\nto a Container-like structure.\n\n\nEach mapping defines exactly one path that may match multiple container-like fragments within the workload\nresource. For each object matching the path the name, env and volumeMounts expressions are resolved to find those\nstructures.",
                  "properties": {
                    "env": {
                      "description": "Env is a Restricted JSONPath that references the slice of environment variables for the container with the\ncontainer-like workload resource fragment. The referenced location is created if it does not exist. Defaults\nto `.envs`.",
                      "type": "string"
                    },
                    "name": {
                      "description": "Name is a Restricted JSONPath that references the name of the container with the container-like workload resource\nfragment. If not defined, container name filtering is ignored.",
                      "type": "string"
                    },
                    "path": {
                      "description": "Path is the JSONPath within the workload resource that matches an existing fragment that is container-like.",
                      "type": "string"
                    },
                    "volumeMounts": {
                      "description": "VolumeMounts is a Restricted JSONPath that references the slice of volume mounts for the container with the\ncontainer-like workload resource fragment. The referenced location is created if it does not exist. Defaults\nto `.volumeMounts`.",
                      "type": "string"
                    }
                  },
                  "required": [
                    "path"
                  ],
                  "type": "object"
                },
                "type": "array"
              },
              "version": {
                "description": "Version is the version of the workload resource that this mapping is for.",
                "type": "string"
              },
              "volumes": {
                "description": "Volumes is a Restricted JSONPath that references the slice of volumes within the workload resource. Defaults to\n`.spec.template.spec.volumes`.",
                "type": "string"
              }
            },
            "required": [
              "version"
            ],
            "type": "object"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  }
}